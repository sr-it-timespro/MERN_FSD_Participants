{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch ./todos-data.json --port 4000",
    "start-with-delay": "json-server --watch ./todos-data.json --port 4000 --delay 4000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "json-server": "^0.17.1"
  }
}
